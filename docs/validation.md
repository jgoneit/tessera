# Tessera v0.1 검증 기록

상태: 구현 및 검증 진행 중. 아래 항목을 모두 확인하기 전에는 전체 v0.1 완료로 간주하지 않습니다.
사용자의 “잘된다” 확인은 이전 아이콘·상하 이동 버전의 기본 동작에 대한 기록입니다.
이전 네 방향 전역 단축키 버전은 해당 번들의 접근성 재등록 후 사용자가 “이제 이동함”으로 기본 이동을 확인했습니다.
이전 다중 격자 버전은 테스트 131개와 release 번들 검증, Seal Acceptance를 통과했습니다. 실제 로그에서
2×2+3×2 후보 요청과 일부 창 배치 성공을 확인했지만, 오른쪽 가장자리에서 2×2로 넓힐 때 제약 결과가 반복됐습니다.
이전 가장자리 변경은 크기 변경 전에 필요한 공간을 확보하는 수정입니다. 해당 수정은 자동 테스트 158개와 release 번들
생성·서명 검증을 통과했습니다. 접근성에 새 번들을 재등록한 뒤 ChatGPT 창의 오른쪽 ⅓ → 오른쪽 절반 확대를
실제 프레임 로그와 사용자 “이제 오른쪽 절반으로 넓어짐” 답변으로 확인했습니다.
설정·선택기·결과 안내의 UI와 한국어·영어, 시스템·라이트·다크 테마를 지원합니다.
메뉴바에는 단색 template 아이콘 하나를 표시합니다. 현재 변경은 설정 헤더·권한 상태·여백의 압축,
키캡 배경의 약한 음영과 선택기의 반응형 조작 안내입니다.
후속 로컬 설치 변경은 헤더 설명 문구 제거, 응용 프로그램 폴더 설치와 ad hoc DMG 포장입니다.
이전 버전의 성공을 이번 번들이나 모든 다중 격자 조합의 통과 증거로 확대하지 않습니다.

## 환경

- macOS 26.6.2, Apple Silicon
- Swift 6.4, Command Line Tools
- 최초 탐지 화면: TFG40U12PW 한 대, 5120×2160pt, scale 1, visible frame `(0, 0, 5120, 2130)`
- 최소 지원 버전 설정: macOS 14. 실제 macOS 14에서의 실행은 별도 미검증입니다.

## 로컬 설치와 DMG — 2026-09-19

- 설정의 로고·앱 이름 아래 설명 문구와 사용하지 않는 한영 번역 항목을 제거했습니다.
  새 빌드의 접근성 트리에서 헤더가 Tessera·버전만 포함하는 것을 확인했습니다.
- `TESSERA_UI_PREVIEW_DIR="$PWD/.build/install-previews" swift test`: 174개(Core 57, App 117) 통과.
  한국어 최소 크기 설정의 대표 렌더에서 설명 제거 후 배치를 확인했습니다.
  `bash scripts/build-app.sh`: release 앱·번역 리소스·아이콘·Info.plist·ad hoc 서명 검증 통과.
- 교체 전 개발용 앱은 `.build/tessera-before-install.qbcvqc_x/Tessera.app`에 보존했습니다.
  `/Applications/Tessera.app`에 복사하고 서명을 검사했으며 원본과 설치본 실행 파일의 SHA-256이 일치합니다.
  기존 개발 경로 프로세스를 종료한 뒤 설치 경로에서 실행 중인 프로세스를 확인했습니다.
- 사용자가 macOS 인증을 완료한 뒤 기존 Tessera 접근성 항목을 `/Applications/Tessera.app`으로 갱신했습니다.
  시스템 설정의 허용 표시와 앱의 ‘창을 배치할 준비가 되었어요’ 상태를 확인했습니다.
  교체 전후 격자·간격·언어·테마·네 방향 키 값이 동일합니다(단축키 JSON은 객체 내용 비교).
- `bash scripts/build-dmg.sh --skip-build`: `dist/Tessera-0.1.0-arm64.dmg` 생성 및 `hdiutil verify` 통과.
  크기는 2,158,858 bytes, SHA-256은 `709094ade6dc9ce83f1c38382a33fdc966f0e7e5c7b893bfa7f0d4e468c1f1a8`입니다.
  읽기 전용 마운트에서 앱 서명·설치본과의 파일 일치·Applications 링크·한영 설치 안내를 확인하고 추출했습니다.
- DMG는 로컬 ad hoc Apple Silicon 빌드입니다. Developer ID 서명·Apple 공증·웹 업로드는 수행하지 않았습니다.
  인터넷 다운로드 후 Gatekeeper 동작, 다른 Mac 설치, Intel·macOS 14 실제 실행은 별도 미검증입니다.
  이번 설치본의 실제 창 배치 전체와 기존 다중 화면 실기도 미완료로 유지합니다.
- 기존 Seal Task·Run은 보존하며 이 후속 변경의 Acceptance로 재사용하지 않습니다.
  미추적 사용자 시안이 있는 worktree이므로 이번 요청의 Seal 자동 활성화는 생략했습니다.

## 자동 검증

| 항목 | 결과 |
| --- | --- |
| 기반 번들 메타데이터 테스트 | 통과 |
| 기반 release .app 생성·ad hoc 서명 검증 | 통과 |
| 아이콘·상하 이동 버전 테스트 | 68개 통과 (Core 38, App 30; 일부 parameterized case 포함) |
| 이전 네 방향 전역키 통합 테스트 | `swift test` 107개 통과 (Core 38, App 69; 일부 parameterized case 포함) |
| 이전 네 방향 전역키 release 번들 | `bash scripts/build-app.sh` 통과. ICNS 생성·Info.plist 검사·ad hoc 서명 및 서명 검증 통과 |
| 이전 다중 격자 통합 테스트 | `swift test` 131개 통과 (Core 48, App 83; 일부 parameterized case 포함). 선택 조합 7개와 2×2 Geometry 포함 |
| 이전 다중 격자 release 번들 | `bash scripts/build-app.sh` 통과. ICNS·Info.plist·ad hoc 서명 검증 통과 |
| 이전 다중 격자 Seal | `TESSERA-V01-MULTIGRID-20260917`, Run `7319c7d0498745209bacc9a9d9826944` accepted |
| 가장자리 크기 변경 수정 테스트 | `swift test` 158개 통과 (Core 57, App 101; 일부 parameterized case 포함). 경계에서 폭·높이 확대, 취소·실패 후 쓰기 중단, 읽기 실패와 부분 변경 처리 포함 |
| 가장자리 크기 변경 수정 번들 | `bash scripts/build-app.sh` 통과. ICNS·Info.plist·ad hoc 서명 검증 통과 |
| 이전 가장자리 Seal | `TESSERA-V01-EDGE-RESIZE-20260918`, Run `b64e6869e09b47d88410d9dd31ff4d3e` accepted |
| 이전 Seal Task·Run | `TESSERA-V01-20260917`, `TESSERA-V01-ARROWS-20260917`, `TESSERA-V01-ICON-NAV-20260917`, `TESSERA-V01-DIRECTIONAL-20260917`, `TESSERA-V01-MULTIGRID-20260917` 및 기존 Run 보존 |
| 이전 UI 변경의 Seal | 명시 선택 없는 새 요청이며 기존 worktree가 dirty이므로 자동 활성화 생략. 기존 Task·Run 보존. 테스트·번들 검사는 직접 실행 |
| UI·언어·테마 자동 검사 | `TESSERA_UI_PREVIEW_DIR="$PWD/.build/ui-previews" swift test` 174개 통과 (Core 57, App 117; offscreen render 2개 포함). en/ko 각 179개 번역 키 검증 |
| UI release 번들 | `bash scripts/build-app.sh` 통과. 번역 리소스 en/ko 포함·문법 검사, ICNS·Info.plist·ad hoc 서명 검증 통과 |

## 설정·선택기 UI 다듬기

- 설정의 기본 680×800pt·최소 640×620pt·스크롤을 유지하고 바깥 여백 24pt, 섹션 간격 16pt,
  헤더 아이콘 48pt·이름 22pt를 적용했습니다. 권한 허용은 작은 상태 행, 미허용은 설명 카드로 구분합니다.
  두 상태에서 설정 열기·다시 확인 버튼과 접근성 이름을 유지합니다.
- 화면 표시·단축키 카드의 내부 여백은 16pt, 격자 카드는 기존 20pt입니다. 단축키 접힘과 오류 시 펼침,
  녹화·적용·저장 동작은 유지합니다. 키캡은 배경에만 1pt 아래 방향의 약한 그림자를 추가하며,
  대비 증가에서는 그림자를 없애고 테두리를 강조합니다.
- 선택기는 중복 높이 배지를 제거하고 현재 배치 문구와 28pt의 두 줄 상태 공간을 유지합니다.
  네 묶음의 조작 안내가 폭에 따라 한 줄 또는 두 줄로 표시됩니다. 숫자 범위는 격자에 맞춰 1–4/6/8로 바뀝니다.
  en/ko 문구를 함께 추가했으며 Core·AX·키 입력·저장 형식·메뉴 아이콘은 변경하지 않았습니다.
- 사용자 시안 `docs/design_prototype.html`은 로컬 원본 그대로 보존하며 버전 관리·PR에서 제외합니다.
  SHA-256: `d50e0cfc1acacb515e2bb41633e22529ca2c72cb2ec4ce26aa2ea4726493b26b`.
- `TESSERA_UI_PREVIEW_DIR="$PWD/.build/ui-polish-previews" swift test`에서 174개(Core 57, App 117)가 통과했습니다.
  기본·최소·전체 높이 설정, 세 격자, 긴 앱/화면 이름과 제약 문구, 넓은/좁은 선택기를 포함한 PNG 52개를 생성했습니다.
  한영·명암의 대표 이미지에서 설정 컨트롤, 전체 열 외곽선, 한 줄/두 줄 조작 안내가 겹치지 않음을 확인했습니다.
  최소 높이에서 아래 설정은 스크롤 영역에 남으며 같은 폭의 전체 높이 이미지에서도 확인했습니다.
- 고대비 NSAppearance로도 이미지를 생성했습니다. 이는 실제 OS 접근성 설정에서의 SwiftUI 환경 전달 확인과 구분합니다.
  앱 활성화·단축키 등록·사용자 설정 변경을 하지 않는 렌더 검증입니다.
- 새 Basic Task는 `TESSERA-UI-POLISH-20260918`이며 risk=low, verifier.required=false,
  기존 두 필수 검사·각 300초 제한을 사용합니다. 이전 Task·Run은 보존합니다.
- Seal Run `d22f129812694645a1fa0a1ae91341c6`에서 `unit-tests`·`app-bundle`이 통과했고,
  반환된 정확한 Run ID로 Basic Completion을 받았습니다. 아래 실행 결과는 검증 후 추가한 기록이며,
  해당 UI-polish 실기 검증 동안 앱 코드를 바꾸거나 번들을 다시 빌드하지 않았습니다.
- 교체 전 앱은 `.build/tessera-before-ui-polish.EHperw/Tessera.app`에 보존했습니다.
- 검증된 `dist/Tessera.app`을 실행하고 기존 Tessera 접근성 항목을 현재 번들로 재등록했습니다.
  시스템 설정의 허용 상태와 실행 앱의 ‘창을 배치할 준비가 되었어요’ 상태 행,
  설정 열기·다시 확인 버튼 및 정상 등록된 단축키의 기본 접힘을 확인했습니다.
- 실제 설정 창의 기본 680×800pt와 최소 640×620pt에서 격자·간격·화면 표시 컨트롤을 확인했습니다.
  최소 크기에서 단축키를 펼치고 아래로 스크롤해 네 방향 녹화 항목·설명·적용 버튼에 접근할 수 있었습니다.
  확인 후 기본 크기, 스크롤 상단, 단축키 접힘 상태로 복원했습니다.
- macOS ‘대비 증가’를 켜면서 함께 적용된 ‘투명도 줄이기’ 상태에서 실행 앱의 라이트·다크 설정 화면을 확인했습니다.
  카드·컨트롤·공통 키캡의 경계와 글자가 구분됐습니다. 검증 후 두 OS 설정을 모두 기존 off로 복원하고,
  앱 테마도 시스템으로 복원했습니다. 실제 선택기의 고대비 표시 전체를 직접 검증한 것으로 확대하지 않습니다.
- 교체 전후 UserDefaults를 비교해 2×2+3×2, 간격 0, 시스템 언어·테마와 네 방향 단축키가 유지됨을 확인했습니다.
  단축키는 저장 JSON의 키 순서를 제외한 내용으로 비교했습니다.
- 이번 표현 변경의 실제 전역 물리 키·선택기 포커스·모든 창 제약 및 다중 화면 실기는 별도 미완료입니다.
  이전 번들의 사용자 배치 확인과 이번 UI·권한·설정 보존 확인을 구분하며 전체 v0.1 완료로 표시하지 않습니다.

## 방향 단축키 펼침 영역 보강

- 작은 기본 disclosure 화살표 대신 24pt 배경의 화살표와 최소 44pt 본문 높이·16pt 여백을 가진
  전체 행 버튼을 사용합니다. 접힌 카드 높이는 76pt이며 제목·상태·빈 공간·여백이 같은 버튼 영역입니다.
- `DisclosureGroupStyle`에서 기존 `isExpanded` Binding을 사용해 접을 때 녹화를 취소하는 동작과
  오류 시 펼침·초안 보존을 유지합니다. 펼친 내용은 별도 영역이며 한국어·영어 접근성 펼침 상태를 제공합니다.
- `TESSERA_UI_PREVIEW_DIR="$PWD/.build/disclosure-previews" swift test` 174개가 통과했습니다.
  기존 렌더 검사로 좁은 설정 화면의 펼친 헤더·네 방향 입력·적용 버튼 배치를 확인했습니다.
- 기존 UI 변경과 이번 후속 수정을 포함한 Basic Task는 `TESSERA-UI-DISCLOSURE-20260918`입니다.
  risk=low, verifier.required=false, `unit-tests`·`app-bundle` 각각 300초 제한을 유지합니다.
  이전 Task·Run과 사용자 디자인 시안은 보존합니다.
- 교체 전 번들은 `.build/tessera-before-disclosure.nohu4650/Tessera.app`에 보존했습니다.
- Seal Run `abed48a491b741848bd1f024524c6f3b`의 필수 검사 두 개가 통과했고 정확한 Run ID로 complete했습니다.
  이 아래는 검증 후 추가한 실기 기록이며 이후 앱 코드·번들은 변경하지 않았습니다.
- 검증된 앱을 실행하고 동일 Tessera의 기존 접근성 승인을 갱신해 준비 상태를 확인했습니다.
  실행 앱에서 헤더 가운데의 빈 공간 클릭으로 펼침, 버튼 클릭으로 접힘, 접근성 트리의 제목·상태·펼침 값을 확인했습니다.
  녹화를 시작한 상태에서 접고 다시 펼치면 기존 키 표시로 돌아오고 적용 버튼이 비활성인 것도 확인했습니다.
- 교체 전후 격자·간격·언어·테마·네 방향 키가 유지됐습니다. 단축키 JSON은 객체 내용으로 비교했습니다.
  검증 후 단축키를 접고 스크롤을 상단으로 복원했습니다.
- 현재 OS의 Tab 탐색으로 헤더 포커스를 확보하지 못해 Tab/Space와 VoiceOver 전체 조작은 별도 미검증입니다.
  사용자 키보드 탐색 설정은 변경하지 않았으며 네이티브 Button 동작을 사용합니다. 기존 다중 화면 미완료 상태도 유지합니다.

## 메뉴바 단색 아이콘

- 제공된 단색 참조의 세 칸 모양을 18pt AppKit 벡터 이미지로 그립니다. 투명 배경에 불투명한 모양을 사용하고
  `isTemplate = true`로 지정해 macOS 메뉴바가 색상을 처리하게 합니다.
- 정사각형 상태 항목에 이미지만 표시합니다. 앱 이름 텍스트를 제거하고 Tessera 접근성 이름과 번역된 툴팁은 유지합니다.
- 앱 번들·설정 화면의 컬러 아이콘과 창 배치 동작은 유지합니다.
- 명시적으로 선택한 Seal의 새 Task `TESSERA-V01-MENU-ICON-20260918`에 누적 제품 변경 전체를 포함합니다.
  기존 Task·Run은 보존하며, 같은 두 필수 검사와 각각 300초 제한을 사용합니다.
- Seal Run `de1587e1b97a4d488344bac9802abf22`가 두 필수 검사를 통과했고 정확한 Run ID로 Basic Completion을 받았습니다.
  이 기록은 검증 후 추가한 실행 결과이며, 코드 변경 없이 검증된 `dist/Tessera.app`을 실행했습니다.
- 교체 전 번들은 `.build/tessera-before-menu-icon.yri4lx/Tessera.app`에 보존했습니다.
  새 앱에서 기존 2×2+3×2·간격 0·시스템 언어/테마·단축키 등록 상태가 유지됨을 확인했습니다.
- 새 ad hoc 빌드의 기존 Tessera 접근성 항목을 재등록했습니다. 사용자가 macOS 인증을 완료한 뒤
  시스템 설정의 on 표시와 실행 앱의 ‘다시 확인’ → ‘창을 배치할 준비가 되었어요’ 상태를 확인했습니다.
- UI 도구는 앱 설정 창만 캡처했고 SystemUIServer 연결은 timeout으로 끝나 메뉴바 자체의 시각 검사는
  수행하지 못했습니다. 메뉴바의 light/dark 표시·선택 상태와 VoiceOver는 별도 확인 전입니다.

## UI·언어·테마 검증

- 격자를 미리보기 카드로 선택하고, 간격·테마·언어를 같은 설정 화면에서 조절합니다.
  정상 등록된 단축키 사용자화는 접어 두고, 오류·미등록 시 펼칩니다. 접을 때 녹화를 취소하고 초안은 보존합니다.
- 언어는 시스템·한국어·English, 테마는 시스템·라이트·다크이며 즉시 적용하고 재시작 후 유지합니다.
  손상된 값은 시스템으로 복구합니다. 전환 시 기존 격자·간격·네 방향 키는 유지합니다.
- 메뉴·선택기·결과/오류 안내·녹화기와 접근성 이름도 번역합니다. macOS 14 배포 기준은 유지합니다.
- 영어·한국어의 키 및 format placeholder 일치, 코드의 literal 번역 호출 누락,
  선호 언어 선택·저장·복원·손상값 복구와 녹화 취소를 자동 검증합니다.
- `PresentationRenderTests`는 별도 UserDefaults와 표시하지 않는 NSWindow/NSHostingView로 PNG를 생성합니다.
  앱을 활성화하거나 단축키를 등록하지 않으며, 사용자의 창과 설정을 변경하지 않습니다.
  이는 실제 화면에서의 포커스·클릭·시스템 테마 변경 검증과 구분합니다.
- 최초 통합 검사에서 오류 코드에 로캘의 천 단위 쉼표가 붙는 문제를 발견해 코드 원형을 유지하도록 고쳤습니다.
  영문 Space를 고정 기대하던 기존 테스트는 현재 언어의 키 이름을 기대하도록 갱신했습니다.

자동 검사 174개가 통과했으며 `.build/ui-previews/`에 PNG 20개를 생성했습니다.
한국어·영어의 설정 light/dark, 2×2 단일 영역과 3×2 열 전체 선택기, 긴 제약 알림 등의 대표 이미지를
직접 검사해 텍스트 겹침·잘림과 선택 강조를 확인했습니다. 전체 열의 내부 테두리는 중립색으로 조정해
연속된 외곽선과 단일 칸 선택이 더 분명히 구분되도록 했습니다.

2026-09-18 현재 Mac의 새 release 앱에서 다음을 직접 확인했습니다.

- 한국어 시스템 테마 화면, 라이트 전환과 영어 전환, 영어 다크 화면의 실제 표시·가독성 확인.
- 언어를 English, 테마를 Dark로 저장한 후 앱 프로세스를 종료·재실행해 같은 값과 영어 창 제목 유지 확인.
- 검증 후 언어·테마를 모두 시스템으로 복원해 한국어 표시 확인. OS 테마 자체를 바꾸지는 않았습니다.
- 새 격자 카드에서 2×2를 해제하면 마지막 3×2 항목이 disabled가 되는 것을 확인하고 2×2+3×2로 복원.
  기존 간격 0 및 네 방향 단축키 유지. 단축키 사용자화 펼침·접힘과 영어 녹화 안내 확인.
- 시스템 설정에서 이전 Tessera 항목만 현재 `dist/Tessera.app`으로 재등록. on 표시 후 Tessera의
  ‘다시 확인’을 눌러 ‘창을 배치할 준비가 되었어요’ 상태로 바뀌는 것을 확인.
- 교체 전 번들은 `.build/tessera-before-ui.e6GiHv/Tessera.app`에 보존했습니다.

이번에 Tessera 자체 UI 연결이 정상 동작했습니다. 이전 버전의 도구 연결 실패 기록은 과거 사실로 보존합니다.
현재 UI 번들의 전역 물리 키·선택기 포커스·실제 제약별 안내·OS 테마 자동 전환·VoiceOver 사용 전체와
다중 화면 검증은 별도 미완료입니다. 기존 가장자리 확대의 사용자 확인은 이전 번들의 증거입니다.

## 실제 macOS 검증

| 항목 | 절차 | 결과 |
| --- | --- | --- |
| 메뉴바·설정 | 실행 후 메뉴바 상주와 Settings 표시 | 새 UI 설정·사용자 아이콘·두 언어·세 테마 선택·네 방향 사용자화 펼침/접힘 확인. 메뉴 직접 조작은 새 UI 버전 별도 대기 |
| 격자 복수 선택 | 메뉴와 설정에서 2×2·3×2·4×2 선택, 마지막 항목 해제 차단, 재시작 유지 | 새 UI의 2×2+3×2, Gap 0 유지 및 재시작 확인. 카드에서 마지막 3×2 해제 차단 확인. 모든 조합의 실제 배치는 별도 대기 |
| 공간 순서 이동 | 선택한 격자들의 열 중심 순서, 좌우 순환, 격자 변경 시 높이 유지, 상하 동일 격자 유지 | 수정 번들의 ChatGPT 창에서 2×2+3×2, Gap 0 좌우 전환·순환·위/전체/아래 단계의 실제 배치 성공 확인. 모든 조합·반복·입력 상태 검증은 별도 대기 |
| 오른쪽 가장자리 확대 | 3×2 오른쪽 열에서 2×2 오른쪽 열로 이동하고 실제 프레임 확인 | 수정 번들에서 열 전체와 아래칸의 사전 이동→너비 확대→최종 프레임 일치 확인. 사용자 “이제 오른쪽 절반으로 넓어짐” 확인 |
| 첫 입력·선택기 전환 | 임의 창 중심에서 첫 좌우 입력, 일치 프레임 복원, 격자별 4/6/8개 숫자·클릭 및 표시 일치 | 새 다중 격자 버전 미검증 |
| 설정 중 취소 | 입력 또는 AX 배치 도중 선택 격자·Gap 변경 후 추가 쓰기 중단 | 새 다중 격자 버전 미검증 |
| 권한 없음 | 허용 전 Arrange 요청, 설정 안내·무충돌 확인 | 이전 네 방향 버전에서 전역키 수신 후 `permission required`로 배치를 중단하고 설정을 여는 로그 확인 |
| 권한 허용·철회 | 접근성 허용 후 Check Again, 철회 후 배치 차단 | 2026-09-18 가장자리 수정 번들 재등록 후 시스템 설정 on 및 실제 배치 성공 확인. 배치 중 철회는 대기 |
| 이전 버전 기본 동작 | 아이콘·상하 이동 수정 앱에서 실제 키 입력 | 사용자가 최신 “잘된다”로 기본 동작 확인. 이전 “이동 안 함” 보고 뒤의 확인이며, 모든 세부 시나리오 통과를 뜻하지 않음 |
| 네 방향 직접 배치 | ⌃⌥←/→/↑/↓로 선택기 없이 배치, 원래 앱 포커스·타이핑 유지 | 수정 번들의 ChatGPT 창에서 네 방향 실제 배치 성공 로그 확인. 포커스·타이핑 유지의 별도 검증은 대기 |
| 결과 표시 | 정확한 배치 1초, 오류·앱 제약 4초 안내 | 새 버전 미검증 |
| 전역키 반복 | 좌우 길게 누르기와 키·보조키 해제, 상하 한 번당 한 단계 | 새 버전 미검증 |
| 연속성과 AX | 같은 창 연속 입력, 크기 제한, 수동 이동·크기 변경 및 다른 창 전환 | 이전 네 방향 버전의 Zone 2 → Column 2 full height → Zone 5 적용 성공, 좌우 순환·제약 후 연속 배치 확인. 이전 다중 격자는 일부 전환 성공 확인. 이번 수정과 수동 프레임 변경·창 전환 조합은 대기 |
| 메뉴 선택기 | Arrange Window…에서만 열기, 일반 방향키·전역 방향키·Enter/Esc | 새 경로 미검증 |
| 클릭·숫자 | 2×2·3×2·4×2 각 Zone에서 원래 창 배치 후 선택기 닫기 | 이전 기본 조작 사용자 확인과 별개로 새 버전 전체 Zone 조합은 대기 |
| 입력·취소 | 한글 입력·숫자패드·바깥 클릭·전환, 선택기 숫자 누출 없음 | 세부 조합 대기 |
| 네 방향 설정 | 네 draft 녹화·전체 Apply·맞바꾸기·중복·시스템/다른 앱 충돌·재시작 | 새 버전 미검증. 이전 단일 키 저장·재시작 유지 확인과 구분 |
| 녹화 중 등록키 | 기존 키를 유지한 상태에서 등록키를 눌러 draft만 바꾸기 | 새 버전 미검증 |
| 저장값 이전 | 이전 단일 키가 네 기본키로 전환되고 Layout·Gap 유지 | 자동 테스트 통과, 실제 업그레이드·재시작 확인 대기 |
| 앱 제약 | Terminal 크기 단위, 최소 크기 큰 앱, 고정 크기·모달 | 대기 |
| 대상 소실 | 창 닫힘, 앱 전환, 전체 화면에서 안전한 결과 | 대기 |
| 내장·외부 화면 | 각각의 화면에서 배치 | 환경 준비 대기 |
| 상대 원점·배율 | 외부 화면 좌우·위아래 배치와 서로 다른 배율 | 환경 준비 대기 |
| Dock·메뉴바 | Dock 좌·우·하단 및 자동 숨김에서 visible frame 확인 | 대기 |
| 화면 연결 변경 | 선택기·직접 배치 도중 외부 화면 분리, 배치 취소 | 환경 준비 대기 |

실기 검증은 일반 편집기나 Finder의 임시 창으로 수행합니다. 실제 사용자 작업 중인 창의 내용은 수정하지 않습니다.
OS 권한과 물리적 화면 연결이 필요한 항목은 사용자가 환경을 준비한 후 이어서 검증합니다.

이전 네 방향 전역키 release 앱 프로세스 실행 후 UI 제어 도구의 `Sky Computer Use native pipe closed before response` 오류로
처음에는 권한 갱신을 수행하지 못했습니다. 후속 실행에서 시스템 설정 연결이 복구되어 기존 Tessera 항목만
제거하고 `/Users/jgoneit/project/tessera/dist/Tessera.app`을 다시 추가했습니다. 시스템 설정의 on 표시를 확인했고,
이후 실제 프로세스 로그에 배치 요청과 `Window arranged.` 최종 프레임 확인이 남았습니다. 사용자는
“이제 이동함”으로 직접 이동을 확인했습니다. Tessera 자체 설정 화면을 읽는 UI 도구 오류는 남아 있으므로
설정 화면 실기 검증까지 통과한 것으로 확대하지 않습니다.

2026-09-18 다중 격자 release 프로세스의 실행과 네 Carbon 단축키 등록을 로그로 확인했습니다. 현재 번들을 같은
dist 경로로 접근성에 다시 등록하고 시스템 설정의 on 표시를 확인했습니다. Tessera 자체 UI 연결 오류는 계속되어
사용자에게 Settings의 2×2+3×2 선택과 실제 공간 순서 이동 확인을 요청했습니다. 이전 작동 버전은
`.build/tessera-before-multigrid.Qeb7dy/Tessera.app`에 보존했습니다.

이후 사용자는 “2×2 오른쪽이 안됨”을 보고했습니다. 실제 로그에서 선택 목록이 `twoByTwo`·`threeByTwo`,
Gap이 0인 것을 확인했습니다. 3×2 오른쪽 열에서 2×2 오른쪽 열로 넓히는 요청은 제약 결과가 자주 나왔고,
3×2 가운데 열에서 같은 2×2 오른쪽 열로 가는 요청과 2×2 왼쪽 열 배치는 적용에 성공했습니다.
후보 선택과 일부 실제 이동의 증거는 있지만, 오른쪽 가장자리 배치가 통과한 것은 아닙니다.
현재 원점에서 크기를 먼저 늘리면 화면 경계를 넘는 경로를 이번 수정의 대상으로 삼았으며,
수정 후 실제 앱의 크기 제한과 구분해 다시 확인해야 합니다.

자동화 도구의 전역 키 입력은 초기 검사에서 TextEdit에 제어 문자로 전달되어 즉시 되돌렸습니다.
따라서 해당 입력을 Carbon 전역 단축키의 실기 통과 근거로 사용하지 않습니다.
사용자의 실제 키 입력과 앱 로그를 함께 확인하며, 이전 버전에 대한 사용자 확인을 새 다중 격자 동작의 증거로 확대하지 않습니다.

## 화면 가장자리 크기 변경 수정

현재 Task는 `TESSERA-V01-EDGE-RESIZE-20260918`입니다. 기존 다중 격자 탐색과 직렬 대기열은 유지하고,
크기 변경에 필요한 공간을 확보하는 선택적 사전 이동을 AX 배치 절차에 추가합니다.

1. 초기 프레임과 요청 프레임을 기준으로, 현재 원점에서 요청 크기가 화면의 visible frame을 벗어나는지 판단합니다.
2. 공간이 부족하면 현재 크기와 요청 크기 각각의 최댓값으로 너비·높이 범위를 잡고, 그 범위에 맞게
   **현재 원점**을 화면 안으로 보정하여 한 번 이동합니다. 최종 위치는 크기를 확인한 뒤 결정합니다.
3. 요청 크기를 한 번 적용한 뒤 실제 크기를 읽습니다.
4. 실제 크기를 기준으로 요청 위치를 화면 안으로 보정하고 한 번 이동한 뒤 최종 프레임을 읽습니다.
5. 요청·실제 프레임을 비교해 적용 또는 앱 제약을 판정합니다. 화면보다 큰 최소 크기는 계속 제약으로 처리합니다.

사전 이동을 포함해 위치·크기 쓰기는 최대 세 번이며 재시도하지 않습니다. 각 쓰기 전에 대상과 취소 상태 등
기존 유효성 검사를 수행합니다. 쓰기를 한 번이라도 시도한 뒤 실패하면 부분 변경 가능성을 알리고 중단합니다.
실패한 AX 쓰기도 부수 효과가 없다고 단정하지 않으며, 자동 원복은 하지 않습니다.

로컬 로그에는 PID와 `requested`·`initial`·`prepared`·`resized`·`final` 프레임을 남겨 공간 확보 여부와
크기 변경 후 응답을 구분합니다. 창 제목·문서 내용은 기록하지 않습니다.

자동 테스트에서는 현재 원점의 남은 공간으로 크기를 제한하는 가짜 창으로 이전 순서의 오른쪽 확대 실패를
재현했습니다. 수정된 순서는 오른쪽 열 전체·위칸·아래칸에서 요청 프레임을 적용합니다. 공간이 이미 충분한
중앙·왼쪽 경로는 사전 이동을 생략하고, 실제 최소 크기 제한은 재시도 없이 제약으로 판정합니다.
권한·대상 변경과 Task 취소를 각 쓰기 경계에 주입해 후속 쓰기 중단과 부분 변경 안내도 확인했습니다.
이는 가짜 창의 자동 검증이며 실제 앱 결과는 아래 실기로 구분합니다.

수정 번들은 2026-09-18 00:27에 실행해 네 Carbon 단축키 등록을 로그로 확인했습니다.
기존 Tessera 접근성 항목만 제거하고 같은 `dist/Tessera.app` 경로를 다시 등록해 on 표시를 확인했습니다.
Tessera 자체 UI 연결에는 기존 도구 오류가 남아 있어 사용자에게 문제가 있던 창에서 물리 키로 재검증을 요청했습니다.
교체 전 번들은 `.build/tessera-before-edge-resize.CWFCbj/Tessera.app`에 보존했습니다.

2026-09-18 00:28:50, ChatGPT 창에서 Gap 0의 오른쪽 열 전체 전환을 확인했습니다. AX 좌표 기준
초기 `(3414, 30, 1706, 2130)`에서 사전 이동 `(2560, 30, 1706, 2130)` 후,
요청한 `(2560, 30, 2560, 2130)`으로 크기와 최종 위치가 일치했습니다. 오른쪽 아래칸도
`(3414, 1095, 1706, 1065)` → 사전 이동 → `(2560, 1095, 2560, 1065)`로 적용됐습니다.
사용자는 “이제 오른쪽 절반으로 넓어짐”을 확인했습니다. 중앙 열에서 오른쪽 절반으로 이동하는 경로는
사전 이동 없이 정확히 적용됐고, 왼쪽 확대·축소·좌우 순환 및 아래칸에서 열 전체로 확대하는 로그도 확인했습니다.
이 기록은 해당 앱·화면·Gap 0에 한정하며, 한글 입력·길게 누르기·모든 앱 제약까지 통과한 것으로 보지 않습니다.
로컬 재현 로그는 `.build/tessera-edge-resize-native.log`에 저장했습니다.

실기 검증 범위와 남은 항목은 다음과 같습니다.

- 2×2+3×2, Gap 0에서 3×2 오른쪽 열 → 2×2 오른쪽 열로 이동합니다. 요청한 격자와 최종 너비·위치가
  일치하는지 확인하고 사전 이동·크기 변경 후 프레임 로그를 함께 확인합니다. 열 전체·아래칸은 위 기록으로 확인했으며,
  오른쪽 위칸에서 같은 가장자리 확대 경로는 별도 실기 대기입니다.
- 비교 경로인 3×2 가운데 열 → 2×2 오른쪽 열, 3×2 왼쪽 열 → 2×2 왼쪽 열도 확인합니다.
  이미 공간이 있는 위치에서 불필요한 사전 이동이 발생하지 않는지 확인합니다.
- 같은 경로를 Gap 8에서 확인하고, 넓은 2×2에서 좁은 3×2로 돌아오는 이동도 확인합니다.
- 최소 크기 제한이 있는 창은 정확한 배치와 구분해 제약 안내를 확인합니다. 취소·실패 후 추가 쓰기가
  중단되는지는 자동 테스트 결과와 실제 관찰을 각각 기록합니다.

Gap 8, 위칸의 가장자리 확대, 앱 최소 크기 제약과 실제 취소·실패 주입은 실기 대기입니다.
다중 화면의 음수 원점·혼합 배율 확인도 기존 대기 항목으로 유지합니다.

## 다중 격자 구현과 검증 범위

- 선택 가능한 격자는 2×2·3×2·4×2이며 하나 이상 유지합니다. 초기값은 3×2 하나입니다.
  메뉴와 설정은 같은 선택 목록을 사용하고, Settings에서는 마지막으로 남은 체크박스를 해제할 수 없게 합니다.
- 좌우 후보는 선택한 격자들의 열 중심을 화면 위치 순서로 합칩니다. 예를 들어 2×2+3×2는
  `3×2 1열 → 2×2 1열 → 3×2 2열 → 2×2 2열 → 3×2 3열` 순서입니다.
  상하는 현재 격자·열을 유지하며 위·전체·아래 단계만 바꾸고, 좌우는 그 높이 단계를 유지합니다.
- 현재 프레임이 활성 후보의 위·전체·아래 프레임과 일치하면 그 선택을 복원합니다. 그렇지 않으면
  선택기는 원래 창 중심에 가장 가까운 열 전체를 강조하며, 동률은 왼쪽을 선택합니다. 첫 좌우 입력은
  원래 창 중심에서 입력 방향에 있는 가장 가까운 후보를 고릅니다. 선택기를 열기만 해서는 배치하지 않습니다.
- 선택기는 현재 후보의 격자를 표시합니다. 숫자·숫자패드는 표시 중인 격자의 1–4/1–6/1–8만 선택하며,
  범위 밖 숫자는 소비하되 배치하지 않습니다. 클릭은 표시된 격자와 Zone을 함께 전달합니다.
- `tessera.enabledLayouts.v1`에는 raw value 배열을 2×2·3×2·4×2 순서로 저장합니다. 유효한 중복 값은
  정규화하고, 빈 배열·잘못된 타입·알 수 없는 항목이 있는 배열은 전체를 3×2 하나로 복구합니다.
  새 키가 없을 때만 기존 `tessera.defaultLayout`을 한 개 선택으로 이전하며, 새 목록 저장 후 이전 키를 제거합니다.
  Gap과 네 방향 단축키 저장 계약은 유지합니다.
- 설정 변경 취소는 Coordinator가 공유 설정의 변경을 동기적으로 관찰하는 경로에서 처리합니다.
  Settings만의 지연된 onChange에 의존하지 않으며 메뉴에서 바꿀 때도 같은 경로를 사용합니다.

설정 테스트에는 가능한 일곱 개 선택 조합, 마지막 항목 제거 차단, 중복 정규화, 새 키 우선순위,
기존 단일 값 이전, 손상된 저장값 복구를 추가했습니다. 다중 격자 버전의 자동 검증은 위의 131개 결과와
Seal 기록을 참조합니다. 이전 버전의 오른쪽 확대 실패와 가장자리 수정 후의 성공은 위 실기 기록에서 구분합니다.

## 네 방향 전역 단축키 구현

- 기본값은 Control+Option과 좌·우·상·하 방향키입니다. 직접 배치는 원래 앱의 포커스를 유지하며
  선택기를 열지 않습니다. 성공 안내는 1초, 오류·앱 제약 안내는 4초 표시하도록 구현했습니다.
- 메뉴의 Arrange Window…는 별도의 선택기 경로입니다. 일반 방향키와 숫자·클릭으로 조작하며,
  열린 선택기에서도 등록된 방향 조합이 중복 처리되지 않도록 경로를 구분합니다.
- 좌우 반복은 등록키의 press/release와 자체 타이머로 처리합니다. 상하는 키를 다시 누를 때 한 단계만
  진행합니다. 입력 취소·녹화·등록 변경 후 남은 이벤트와 반복이 새로운 배치로 이어지지 않게 처리합니다.
- 네 설정은 draft를 수정한 뒤 전체 Apply합니다. 후보 등록 실패 시 추가 등록을 정리하고 기존 네 키와
  저장값을 유지합니다. 녹화 중 기존 키의 등록은 유지하며 Carbon 콜백을 활성 recorder의 draft로 전달합니다.
- `tessera.directionalShortcuts.v2`에는 유효한 네 키를 함께 저장합니다. 이전 단일 키는 네 기본값으로
  전환하고 Layout·Gap은 유지합니다. 손상·누락·중복 값은 묶음 전체를 기본값으로 복구합니다.
- 연속 입력의 논리적 위치와 AX에서 확인한 실제 프레임을 분리합니다. 앱 제약으로 실제 크기가 달라도
  수락한 상하 단계를 유지하며, 대상·화면·설정·수동 프레임 변경 시 현재 상태에서 다시 시작합니다.
- AX 준비 중 입력은 순서대로 반영하고 실제 쓰기는 직렬화합니다. 실행 중인 요청 다음에는 마지막
  대기 요청을 적용하며, 대상 소실·실패·강제 취소 후에는 추가 쓰기를 중단합니다.

단축키 등록·실패 원복·이벤트 순서·반복 취소는 가짜 등록 backend와 clock을 사용해 자동 검증합니다.
설정 이전·네 값 영속성·단일 recorder·등록키 draft 전달·한글 물리 키 처리도 자동 테스트에 포함합니다.
지연된 실행기를 이용한 배치 테스트는 동시 작업 수 1, 마지막 대기 요청 처리, 종료·취소·실패 시 정리를
검증합니다. 이 결과는 실제 Carbon 전달, AX 응답, macOS 포커스 유지의 증거와 구분합니다.

최종 프레임 판정은 요청 프레임의 네 경계와 `0.001pt` 이내로 일치할 때만 정확히 적용된 것으로 처리합니다.
fractional 좌표·부동소수점 잡음·2× 화면 subpixel 차이·누적 경계 오차는 기존 회귀 테스트에 포함됩니다.

## 이전 아이콘 및 상하 이동 수정 기록

- 제공된 PNG 원본을 보존하고 앱 번들·메뉴바·설정 아이콘으로 사용합니다.
  `sips`와 `iconutil`로 16–1024px의 표준 1×·2× 표현을 생성했습니다.
- 재열기 시 현재 layout·gap·scale의 위·아래 Zone과 실제 프레임이 일치하면 그 단계에서 이어가도록
  수정했습니다. 상하 자동 반복이 전체 단계를 건너뛰던 경로에는 한 번당 한 단계 제한을 적용했습니다.
- 해당 버전은 `swift test` 68개와 release 번들 빌드·ad hoc 서명 검증을 통과했습니다.
  설정 아이콘을 직접 확인했고, 사용자 인증 후 번들을 재등록해 앱의 접근성 enabled 상태를 확인했습니다.
- 수정본에서 한때 “이동 안 함” 보고와 선택기 표시 후 취소 로그가 있었습니다. 이후 사용자의 최신
  “잘된다”로 이전 버전 기본 동작 확인을 갱신했습니다. 후속 네 방향 전역키 확인은 위 기록을 참조하며,
  세부 취소·다중 화면·새 다중 격자 실기는 별도입니다.
- 해당 변경의 Task는 `TESSERA-V01-ICON-NAV-20260917`이며 이전 Task·Run과 함께 보존합니다.
